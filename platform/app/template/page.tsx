import AITemplateGenerator from "@/components/AITemplateGenerator";

export const metadata = {
  title: "Template Studio — Xquads",
};

export default function Page() {
  return <AITemplateGenerator />;
}
